services:
  mysql:
    image: mysql:8.0
    platform: linux/amd64
    container_name: auth-bridge-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_DATABASE: auth_bridge
      MYSQL_USER: authbridge
      MYSQL_PASSWORD: authbridge
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "authbridge", "-pauthbridge"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  mysql_data:
